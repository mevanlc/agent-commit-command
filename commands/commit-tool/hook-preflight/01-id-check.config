# 01-id-check.config - Identity validation hook configuration
#
# Format: key=value (no spaces around =)
# Lines starting with # are comments
# Blank lines are ignored

# Your known identities (can have multiple)
id=Mike Clark <mevanlc@gmail.com>
id=Mike Clark <mike.clark.8192@gmail.com>
id=Mike Clark <mike.clark@nvoq.com>

# Stop if one of your IDs appears in commit log but you're about to commit as a different one
enforce_one_id_per_repo=1

# Stop if your current email appears in commit log attached to a different name
# (This check runs even without configured IDs - uses git config identity)
enforce_name=1

# Show last N commits from each unique name in your configured IDs (0 to disable)
report_named_commits=3

# Show last N commits from each unique email in your configured IDs (0 to disable)
report_email_commits=3
