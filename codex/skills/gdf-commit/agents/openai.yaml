interface:
  display_name: "Gdf Commit"
  short_description: "Run the gdf-based commit helper"
  default_prompt: "Use $gdf-commit to run the commit review helper through gdf, require an explicit mode such as --staged, --all, or --ask, and wait for user confirmation before creating the commit."
